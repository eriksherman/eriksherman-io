import Layout from 'components/layout'
import SEO from 'components/SEO'
import MATH461Markdown from 'notes/MATH461.md'

<MATH461Markdown />

export default ({ children }) => (
    <Layout>
        <SEO />
        {children}
    </Layout>
)
